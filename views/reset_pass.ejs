<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include("partials/head", {title: title}); %>
  </head>
  <body>
    <%- include("partials/navbar", {loggedIn: false}); %>
    <%- include("partials/messages"); %>
    
    <div class="container-sm mb-5 pt-5">
      <h1 class="display-3 text-center mb-3" style="margin-top: 100px;">Reset your password</h1>
      <hr class="my-5"/>
      <form action="/reset-password" method="post" class="mx-auto" style="max-width: 600px">
      
        <!-- Hidden email and token fields -->
        <input type="hidden" name="email" value="<%= email %>"/>
        <input type="hidden" name="token" value="<%= token %>"/>
        
        <!-- New password input -->
        <div class="row mt-3">
          <div class="col-sm-6">
            <input type="password" class="form-control" id="password" name="password" placeholder="Password" autocomplete="off" required/>
            <label for="password">Password</label>
            <small class="text-muted ms-1">Must be at least 6 characters.</small>
          </div>
        </div>
          
        <!-- Confirm new password -->
        <div class="row mt-3">
          <div class="col-sm-6">
            <input type="password" class="form-control" id="confirm_pw" name="confirm_pw" placeholder="Confirm password" autocomplete="off" required/>
            <label for="confirm_pw">Confirm password</label>
          </div>
        </div>
        
        <!-- Reset password button -->
        <div class="d-grid col-6 mx-auto my-5">
          <button class="btn btn-outline-info" type="submit">Reset Password</button>
        </div>
      </form>
    </div>
  </body>
</html>
